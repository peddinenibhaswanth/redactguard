# Phase 1 + 2 + demo/API - all free/CPU-only. Phase 3 training deps are
# separate (requirements-colab.txt) since that runs on Colab, not here.
langgraph>=0.2.0
langchain-core>=0.3.0
pymupdf>=1.24.0
python-docx>=1.1.0
docx2pdf>=0.1.8
pytesseract>=0.3.10
pillow>=10.0.0
google-genai>=1.0.0
groq>=0.11.0
fastapi>=0.112.0
uvicorn>=0.30.0
python-multipart>=0.0.9
pydantic>=2.8.0
python-dotenv>=1.0.0
filetype>=1.2.0

# demo UI
streamlit>=1.38.0
requests>=2.32.0

# NOTE: Phase 3 local-model inference deps (torch/transformers/peft) are
# deliberately NOT here - see requirements-phase3.txt. They add ~2GB, which
# blows the resource limit on free demo hosts and slows CI, and nothing in
# Phase 1/2, the API, the demo, or the test suite imports them.

# testing
pytest>=8.3.0
httpx>=0.27.0
